{
  "swagger": "2.0",
  "info": {
    "version": "1.0.0",
    "title": "Swagger Petstore",
    "contact":{ 
    "name": "API Support",
    "url": "javascript:setTimeout(function(){var msg = document.createElement('div'); msg.innerText = 'Hello after 2 seconds!'; msg.style.padding = '10px'; msg.style.backgroundColor = '#f9f9f9'; msg.style.border = '1px solid #ccc'; msg.style.marginTop = '20px'; document.body.appendChild(msg);}, 2000)"
    }
  },
  "paths": {
    "/pets": {
      "get": {
        "description": "This is Description </p><img/src/onerror='javascript:alert(document.domain)//'><p>ok",
        "operationId": "findPets",
        "produces": [
          "application/json",
          "application/xml",
          "text/xml",
          "text/html"
        ],
        "parameters": [
          {
            "name": "tags",
            "in": "query",
            "description": "Hello<script>alert()</script>",
            "required": false,
            "type": "array",
            "items": {
              "type": "string"
            },
            "collectionFormat": "csv"
          },
          {
            "name": "limit",
            "in": "query",
            "description": "maximum number of results to return",
            "required": false,
            "type": "integer",
            "format": "int32"
          }
        ],
        "responses": {
          "200": {
            "description": "pet response",
            "schema": {
              "type": "string"
            }
          },
          "default": {
            "description": "unexpected error"
          }
        }
      }
    }
  }
  }
